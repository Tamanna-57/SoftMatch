PORT=3001
CLIENT_URL=http://localhost:5173

# --- Database (not required for demo; see SUGGESTIONS.md) ---
# DATABASE_URL=postgresql://user:pass@localhost:5432/softmatch
# JWT_SECRET=change_this_to_a_random_string

# --- AI Companion "Soft" (optional) ---
# Leave unset to use the built-in offline companion.
# Set this to enable real LLM replies via /api/companion.
# ANTHROPIC_API_KEY=sk-ant-...
